# Packages

Reserved for V2 shared packages defined in issue `#22`.

- `domain/`: typed domain models and schemas
- `retrieval/`: provider interfaces and adapters
- `pipeline/`: normalization and orchestration logic
- `export/`: memo/export rendering
- `evals/`: benchmark fixtures and scorecards
